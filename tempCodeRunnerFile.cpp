void solve() {
    string s;
    cin>>s;
    set<char> u(s.begin(), s.end());
    vector<char> so(u.begin(), u.end());
    sort(s.begin(), s.end());
    string r=string(so.begin(), so.end());
   
   
}